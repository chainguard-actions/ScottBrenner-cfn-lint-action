name: Test command input
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          command: "--version"
      - name: Assert cfn-lint ran successfully
        run: |
          echo "cfn-lint command step completed successfully"
          echo "PASS"
